diff --git a/back-end/back-end/Controllers/Artists.cs b/back-end/back-end/Controllers/Artists.cs
deleted file mode 100644
index 42d9aa3..0000000
--- a/back-end/back-end/Controllers/Artists.cs
+++ /dev/null
@@ -1,20 +0,0 @@
-﻿using back_end.Data;
-using back_end.Database.DbAccess.Interfaces;
-using Microsoft.AspNetCore.Mvc;
-
-namespace back_end.Controllers
-{
-    [Route("[controller]")]
-    [ApiController]
-
-    public class ArtistsController : ControllerBase
-    {
-        private readonly IStatic _dbAccess;
-
-        public ArtistsController(AppDbContext context, IStatic dbAccess)
-        {
-            _dbAccess = dbAccess;
-        }
-
-    }
-}
diff --git a/back-end/back-end/Controllers/Authors.cs b/back-end/back-end/Controllers/Authors.cs
deleted file mode 100644
index 98dfb0a..0000000
--- a/back-end/back-end/Controllers/Authors.cs
+++ /dev/null
@@ -1,18 +0,0 @@
-﻿using back_end.Data;
-using back_end.Database.DbAccess.Interfaces;
-using Microsoft.AspNetCore.Mvc;
-
-namespace back_end.Controllers
-{
-    [Route("[controller]")]
-    [ApiController]
-    public class AuthorsController : ControllerBase
-    {
-        private readonly IStatic _dbAccess;
-
-        public AuthorsController(AppDbContext context, IStatic dbAccess)
-        {
-            _dbAccess = dbAccess;
-        }
-    }
-}
diff --git a/back-end/back-end/Controllers/Titles.cs b/back-end/back-end/Controllers/Titles.cs
index 101e115..a61fb29 100644
--- a/back-end/back-end/Controllers/Titles.cs
+++ b/back-end/back-end/Controllers/Titles.cs
@@ -46,26 +46,26 @@ namespace back_end.Controllers
         [HttpGet("search")]
         public async Task<ActionResult<List<DTOs.Title>>> SearchTitle(
             [FromQuery] string? name,
-            [FromQuery] string[]? authors,
-            [FromQuery] string[]? artists,
+            [FromQuery] string? author,
+            [FromQuery] string? artist,
             [FromQuery] int[]? genresIds,
             [FromQuery] int[]? themesIds,
-            [FromQuery] int? publicationYear,
-            [FromQuery] int? statusId,
-            [FromQuery] int? demographicId,
-            [FromQuery] int? contentRatingId
+            [FromQuery] int[]? demographicIds,
+            [FromQuery] int[]? statusIds,
+            [FromQuery] int[]? contentRatingIds,
+            [FromQuery] int? publicationYear
             )
         {
             Result<List<DTOs.Title>> result = await _dbAccess.GetTitlesByFilters(
                 name,
-                authors,
-                artists,
+                author,
+                artist,
                 genresIds,
                 themesIds,
-                publicationYear,
-                demographicId,
-                statusId,
-                contentRatingId
+                demographicIds,
+                statusIds,
+                contentRatingIds,
+                publicationYear
                 );
 
             if (result.IsFailure)
diff --git a/back-end/back-end/Database/DbAccess/Artist.cs b/back-end/back-end/Database/DbAccess/Artist.cs
deleted file mode 100644
index 29990bd..0000000
--- a/back-end/back-end/Database/DbAccess/Artist.cs
+++ /dev/null
@@ -1,6 +0,0 @@
-﻿namespace back_end.Database.DbAccess
-{
-    public class Artist
-    {
-    }
-}
diff --git a/back-end/back-end/Database/DbAccess/Author.cs b/back-end/back-end/Database/DbAccess/Author.cs
deleted file mode 100644
index 46ce103..0000000
--- a/back-end/back-end/Database/DbAccess/Author.cs
+++ /dev/null
@@ -1,6 +0,0 @@
-﻿namespace back_end.Database.DbAccess
-{
-    public class Author
-    {
-    }
-}
diff --git a/back-end/back-end/Database/DbAccess/Interfaces/ITitle.cs b/back-end/back-end/Database/DbAccess/Interfaces/ITitle.cs
index 615a4b0..eadacc4 100644
--- a/back-end/back-end/Database/DbAccess/Interfaces/ITitle.cs
+++ b/back-end/back-end/Database/DbAccess/Interfaces/ITitle.cs
@@ -12,14 +12,14 @@ namespace back_end.Database.DbAccess.Interfaces
 
         Task<Result<List<DTOs.Title>>> GetTitlesByFilters(
             string? name,
-            string[]? authors,
-            string[]? artists,
+            string? author,
+            string? artist,
             int[]? genresIds,
             int[]? themesIds,
-            int? publicationYear,
-            int? demographicId,
-            int? statusId,
-            int? contentRatingId
+            int[]? demographicIds,
+            int[]? statusIds,
+            int[]? contentRatingIds,
+            int? publicationYear
             );
     }
 }
diff --git a/back-end/back-end/Database/DbAccess/Title.cs b/back-end/back-end/Database/DbAccess/Title.cs
index 1350789..d89ed5e 100644
--- a/back-end/back-end/Database/DbAccess/Title.cs
+++ b/back-end/back-end/Database/DbAccess/Title.cs
@@ -2,6 +2,7 @@
 using back_end.Database.DbAccess.Interfaces;
 using Microsoft.EntityFrameworkCore;
 using back_end.Shared.Core;
+using back_end.Models;
 
 namespace back_end.Database.DbAccess
 {
@@ -75,14 +76,14 @@ namespace back_end.Database.DbAccess
         }
         public async Task<Result<List<DTOs.Title>>> GetTitlesByFilters(
             string? name,
-            string[]? authors,
-            string[]? artists,
+            string? author,
+            string? artist,
             int[]? genresIds,
             int[]? themesIds,
-            int? publicationYear,
-            int? demographicId,
-            int? statusId,
-            int? contentRatingId
+            int[]? demographicIds,
+            int[]? statusIds,
+            int[]? contentRatingIds,
+            int? publicationYear
             )
         {
             try
@@ -93,13 +94,13 @@ namespace back_end.Database.DbAccess
                 if (!string.IsNullOrWhiteSpace(name))
                     query = query.Where(t => EF.Functions.ILike(t.name, $"%{name}%"));
 
-                //? Authors
-                if (authors?.Length >= 1)
-                    query = query.Where(t => authors.All(a => t.authors.Any(aut => EF.Functions.ILike(aut, "%" + a + "%"))));
+                //? Author
+                if (!string.IsNullOrWhiteSpace(author))
+                    query = query.Where(t => t.authors.Any(aut => EF.Functions.ILike(aut, $"%{author}%")));
 
-                //? Artists
-                if (artists?.Length >= 1)
-                    query = query.Where(t => artists.All(a => t.artists.Any(art => EF.Functions.ILike(art, "%" + a + "%"))));
+                //? Artist
+                if (!string.IsNullOrWhiteSpace(artist))
+                    query = query.Where(t => t.artists.Any(art => EF.Functions.ILike(art, $"%{artist}%")));
 
                 //? Genres
                 if (genresIds?.Length >= 1)
@@ -109,21 +110,21 @@ namespace back_end.Database.DbAccess
                 if (themesIds?.Length >= 1)
                     query = query.Where(t => themesIds.All(id => t.themes.Contains(id)));
 
-                //? Publication Year
-                if (publicationYear.HasValue)
-                    query = query.Where(t => t.publicationDate.Year == publicationYear.Value);
-
                 //? Demographic
-                if (demographicId.HasValue)
-                    query = query.Where(t => t.Demographic == demographicId.Value);
+                if (demographicIds?.Length >= 1)
+                    query = query.Where(t => demographicIds.Contains(t.Demographic));
 
                 //? Status
-                if (statusId.HasValue)
-                    query = query.Where(t => t.Status == statusId.Value);
+                if (statusIds?.Length >= 1)
+                    query = query.Where(t => statusIds.Contains(t.Status));
 
                 //? Content Rating
-                if (contentRatingId.HasValue)
-                    query = query.Where(t => t.ContentRating == contentRatingId.Value);
+                if (contentRatingIds?.Length >= 1)
+                    query = query.Where(t => contentRatingIds.Contains(t.ContentRating));
+
+                //? Publication Year
+                if (publicationYear.HasValue)
+                    query = query.Where(t => t.publicationDate.Year == publicationYear.Value);
 
                 List<DTOs.Title> titles = await RunQuery(query);
                 return Result<List<DTOs.Title>>.Success(titles);
diff --git a/front-end/src/Components/Global/Inputs/checkboxinput.scss b/front-end/src/Components/Global/Inputs/checkboxinput.scss
index b01741d..25aebce 100644
--- a/front-end/src/Components/Global/Inputs/checkboxinput.scss
+++ b/front-end/src/Components/Global/Inputs/checkboxinput.scss
@@ -3,19 +3,7 @@
 
 .checkboxinput {
     &-label {
-        position: relative;
-
-        padding: 2px 4px;
-
-        border: 1px solid var(--color2);
-        border-radius: border-radius.$border-radius;
-
         cursor: pointer;
-
-        &--selected {
-            border: 1px solid var(--true);
-            background-color: var(--true-transparent);
-        }
     }
 
     &-input {
@@ -28,13 +16,5 @@
 
         width: 100%;
         height: 100%;
-        
-    }
-
-    &-text {
-        &--selected {
-            color: var(--true)
-        }
     }
-
 }
\ No newline at end of file
diff --git a/front-end/src/Components/Global/Inputs/checkboxinput.tsx b/front-end/src/Components/Global/Inputs/checkboxinput.tsx
index 239d914..f8b2f93 100644
--- a/front-end/src/Components/Global/Inputs/checkboxinput.tsx
+++ b/front-end/src/Components/Global/Inputs/checkboxinput.tsx
@@ -50,17 +50,13 @@ export default function checkboxinput({
     
     const frmtd_classNameLabel: string = `
        checkboxinput-label 
-       ${checked ? 'checkboxinput-label--selected' : ''}
        ${classNameLabel}
        `
     const frmtd_classNameInput: string = `
        checkboxinput-input 
        ${classNameInput}
        `
-    const frmtd_classNameText: string = `
-       checkboxinput-text 
-       ${checked ? 'checkboxinput-text--selected' : ''}
-       `
+    const frmtd_classNameText: string = `checkboxinput-text`
 
     return (
         <>
diff --git a/front-end/src/Components/Global/Titles/featuredtitles.tsx b/front-end/src/Components/Global/Titles/featuredtitles.tsx
index 871c3ca..e949b20 100644
--- a/front-end/src/Components/Global/Titles/featuredtitles.tsx
+++ b/front-end/src/Components/Global/Titles/featuredtitles.tsx
@@ -6,7 +6,7 @@ import Button from '../Inputs/button'
 //# Libs //
 import { useState } from 'react'
 //# Utils //
-import staticMapper from '../../../Shared/utils/staticMapper'
+import { staticMapper } from '../../../Shared/utils/staticHandler'
 //# Types //
 import { title } from '../../../Shared/types/Data/title'
 //# Classes //
diff --git a/front-end/src/Components/Global/Titles/titlesgrid.tsx b/front-end/src/Components/Global/Titles/titlesgrid.tsx
index 7a7ab11..c771dd8 100644
--- a/front-end/src/Components/Global/Titles/titlesgrid.tsx
+++ b/front-end/src/Components/Global/Titles/titlesgrid.tsx
@@ -4,7 +4,7 @@ import Link from '../link'
 import Img from '../img'
 //# Utils //
 import chunkArray from '../../../Shared/utils/chunkArray'
-import staticMapper from '../../../Shared/utils/staticMapper'
+import { staticMapper } from '../../../Shared/utils/staticHandler'
 //# Types //
 import { title } from '../../../Shared/types/Data/title'
 //# Config //
diff --git a/front-end/src/Components/Title/title.tsx b/front-end/src/Components/Title/title.tsx
index 6941815..e1a2d5a 100644
--- a/front-end/src/Components/Title/title.tsx
+++ b/front-end/src/Components/Title/title.tsx
@@ -8,7 +8,7 @@ import { useParams } from 'react-router'
 //# Services //
 import { GetTitleById } from '../../Shared/api/FetchTitle'
 //# Utils //
-import staticMapper from '../../Shared/utils/staticMapper'
+import { staticMapper } from '../../Shared/utils/staticHandler'
 //# Types //
 import { title, titlemetakeys } from '../../Shared/types/Data/title'
 import { staticDataArray } from '../../Shared/types/Data/static'
diff --git a/front-end/src/Components/Titles/search.scss b/front-end/src/Components/Titles/search.scss
index f7e0c03..2be4334 100644
--- a/front-end/src/Components/Titles/search.scss
+++ b/front-end/src/Components/Titles/search.scss
@@ -101,6 +101,8 @@
             width: 100%;
             height: 50%;
 
+            padding-right: 6px;
+            
             transition: filter transition-time.$transition-time ease;
 
             &:focus {
@@ -110,9 +112,6 @@
             span {
                 display: flex;
                 align-items: center;
-
-                margin-left: 2.5%;
-
                 font-size: text.$font-size5;
             }
         }
@@ -177,10 +176,23 @@
                                 width: fit-content;
                                 height: fit-content;
 
-                                input {}
+                                label {
+                                    position: relative;
 
+                                    padding: 2px 4px;
 
+                                    border: 1px solid var(--color2);
+                                    border-radius: border-radius.$border-radius;
 
+                                    &:has(input:checked) {
+                                        border: 1px solid var(--true);
+                                        background-color: var(--true-transparent);
+
+                                        span {
+                                            color: var(--true);
+                                        }
+                                    }
+                                }
 
                             }
                         }
@@ -188,37 +200,74 @@
 
                 }
 
-                &--singleselect {
+                &--multidropdown {
+
+                    /*
+                    &-circle {
+                        display: flex;
+
+                        width: text.$font-size7;
+                        height: text.$font-size7;
+
+                        border: 1px solid black;
+                        border-radius: 50%;
+                    }
+                        */
+
                     ul {
-                        margin: 0;
+                        margin: 0 0 0 7%;
                         padding: 0;
-
                         display: flex;
                         flex-direction: column;
-
-                        margin-left: 7%;
                         gap: 0.25rem;
 
                         li {
+                            display: flex;
+                            justify-content: space-between;
+                            align-items: center;
                             list-style: none;
 
                             label {
                                 display: flex;
-                                width: 100%;
+                                align-items: center;
 
+                                width: 90%;
                                 gap: 5%;
 
-
                                 span {
                                     font-size: text.$font-size5;
                                 }
+
+                                &::before {
+                                    content: "";
+
+                                    display: flex;
+
+                                    width: text.$font-size7;
+                                    height: text.$font-size7;
+
+                                    border: 1px solid black;
+                                    border-radius: 50%;
+
+
+                                }
+
+                                &:has(input:checked) {
+                                    &::before {
+                                        background-color: var(--color3);
+                                        border-color: var(--color3);
+                                    }
+
+                                    span {
+                                        color: var(--color3);
+                                    }
+                                }
                             }
                         }
-
                     }
                 }
 
-                &--selectaddiction,
+                &--selecttext,
                 &--searchnumber {
                     display: flex;
                     align-items: center;
diff --git a/front-end/src/Components/Titles/search.tsx b/front-end/src/Components/Titles/search.tsx
index 2509ffd..ffd38a0 100644
--- a/front-end/src/Components/Titles/search.tsx
+++ b/front-end/src/Components/Titles/search.tsx
@@ -2,11 +2,13 @@
 import Text from '../Global/text'
 import Input from '../Global/Inputs/input'
 import Button from '../Global/Inputs/button'
-import RadioInput from '../Global/Inputs/radioinput'
 import CheckBoxInput from '../Global/Inputs/checkboxinput'
 import TitleGrid from '../Global/Titles/titlesgrid'
 //# Libs //
 import { useState, useEffect, useId, useRef } from 'react'
+import FilterClasses from '../../Shared/utils/FilterClasses'
+import isPlainObject from '../../Shared/utils/isPlainObject'
+import { staticMapper, getAllStaticKeys } from '../../Shared/utils/staticHandler'
 //# Api //
 import { GetTitlesByFilters } from '../../Shared/api/FetchTitle'
 //# Services //
@@ -38,16 +40,16 @@ type Faceted = {
     InputsController: InputsController;
 }
 
-type SingleSelect = {
-    type: 'singleselect';
+type MultiDropDown = {
+    type: 'multidropdown';
     label: string;
     options: staticData[];
     state: state;
     InputsController: InputsController;
 }
 
-type SelectAddiction = {
-    type: 'selectaddiction';
+type SelectText = {
+    type: 'selecttext';
     label: string;
     options?: undefined;
     state: state;
@@ -63,8 +65,9 @@ type SearchNumber = {
 }
 
 //.. Local Components //
-function FilterItem({ type, label, options, state, InputsController }: Faceted | SingleSelect | SelectAddiction | SearchNumber) {
+function FilterItem({ type, label, options, state, InputsController }: Faceted | MultiDropDown | SelectText | SearchNumber) {
     const id = `label_${useId()}`
+
     const { showFilterItem, setShowFilterItem } = state
     const InputName = `search${label.replace(/\s+/g, '').replace(/^./, c => c.toUpperCase())}`
 
@@ -88,19 +91,57 @@ function FilterItem({ type, label, options, state, InputsController }: Faceted |
                     defaultStyle={false}
                     className='search__filters-button'
                 >
-                    <Text tag='span' no_select={true}>
-                        None
+                    <Text tag='span' no_select={true} not_exceed_X={true}>
+                        {
+                            (() => {
+                                const value = InputsController.data[InputName];
+
+                                // MultiDropDown
+                                if (Array.isArray(value) && value.length > 0 && Array.isArray(options)) {
+                                    return value
+                                        .map(v => options.find((obj: staticData) => obj.id === Number(v))?.name)
+                                        .join(", ");
+                                }
+
+                                // SelectText & SelectNumber
+                                if ((typeof value === "string" || typeof value === "number") && !options) {
+                                    return value.toString() || "None";
+                                }
+
+                                // Faceted
+                                if (isPlainObject(options) && Object.values(options).every(Array.isArray)) {
+                                    const optionsKeys = Object.keys(options);
+                                    const staticKeys = getAllStaticKeys()
+
+                                    const values = optionsKeys.flatMap((key) => {
+                                        const frmtdKey = key.toLowerCase() as keyof staticDataArray
+
+                                        if (!staticKeys.includes(frmtdKey)) {
+                                            console.error(`Invalid key "${key}". Cannot format value because it does not exist in data returned by GetAllStatic().`)
+                                            return null
+                                        }
+
+                                        const keyValues = InputsController.data[`${InputName}_${key}`] ?? []
+                                        return keyValues.map((value: string) => staticMapper(frmtdKey, Number(value)))
+                                    })
+
+                                    if (values.length > 0) return values.join(", ")
+                                }
+
+                                return "None";
+                            })()
+                        }
                     </Text>
                 </Button>
                 {showFilterItem === label && (
                     <div
-                        className={`
+                        className={FilterClasses(`
                         search__filters-item__options
                         ${type === "faceted" ? "search__filters-item__options--faceted" : ""}
-                        ${type === "singleselect" ? "search__filters-item__options--singleselect" : ""}
-                        ${type === "selectaddiction" ? "search__filters-item__options--selectaddiction" : ""}
+                        ${type === "multidropdown" ? "search__filters-item__options--multidropdown" : ""}
+                        ${type === "selecttext" ? "search__filters-item__options--selecttext" : ""}
                         ${type === "searchnumber" ? "search__filters-item__options--searchnumber" : ""}
-                        `}
+                        `)}
                     >
                         {
                             type === "faceted" ?
@@ -125,22 +166,22 @@ function FilterItem({ type, label, options, state, InputsController }: Faceted |
                                         </ul>
                                     </section>
                                 ))
-                                : type === "singleselect" ?
+                                : type === "multidropdown" ?
                                     <ul>
                                         {
                                             options.map(option => (
                                                 <li key={option.name}>
-                                                    <RadioInput
-                                                        label={option.name}
+                                                    <CheckBoxInput
                                                         value={option.id}
-                                                        name={InputName}
+                                                        label={option.name}
+                                                        name={`${InputName}`}
                                                         InputsController={InputsController}
                                                     />
                                                 </li>
                                             ))
                                         }
                                     </ul>
-                                    : type == "selectaddiction" ?
+                                    : type == "selecttext" ?
                                         <>
                                             <Input
                                                 type='text'
@@ -186,34 +227,32 @@ export default function Search() {
 
     async function handleSubmit(data: {
         searchName: string,
-        searchAuthors: string[],
-        searchArtists: string[],
+        searchAuthor: string,
+        searchArtist: string,
 
-        searchContentRating: string,
-        searchDemographic: string,
+        searchContentRating: string[],
+        searchDemographic: string[],
+        searchStatus: string[],
         searchPublicationYear: string,
-        searchStatus: string,
 
         searchTags_Genres: string[],
         searchTags_Themes: string[]
     }) {
-
         const res = await GetTitlesByFilters({
             name: data.searchName,
-            authors: data.searchAuthors,
-            artists: data.searchArtists,
+            author: data.searchAuthor,
+            artist: data.searchArtist,
 
-            contentRatingId: data.searchContentRating,
-            demographicId: data.searchDemographic,
+            contentRatingIds: data.searchContentRating,
+            demographicIds: data.searchDemographic,
+            statusIds: data.searchStatus,
             publicationYear: data.searchPublicationYear,
-            statusId: data.searchStatus,
 
             genresIds: data.searchTags_Genres,
             themesIds: data.searchTags_Themes,
 
         })
-        
-        console.log(res)
+
         if (res) setData(res)
 
     }
@@ -276,35 +315,35 @@ export default function Search() {
                                 InputsController={InputsController}
                             />
                             <FilterItem
-                                type={"singleselect"}
+                                type={"multidropdown"}
                                 label="Content Rating"
                                 options={staticData.contentRatings}
                                 state={{ showFilterItem, setShowFilterItem }}
                                 InputsController={InputsController}
                             />
                             <FilterItem
-                                type={"singleselect"}
+                                type={"multidropdown"}
                                 label="Demographic"
                                 options={staticData.demographics}
                                 state={{ showFilterItem, setShowFilterItem }}
                                 InputsController={InputsController}
                             />
                             <FilterItem
-                                type={"singleselect"}
+                                type={"multidropdown"}
                                 label="Status"
                                 options={staticData.statuses}
                                 state={{ showFilterItem, setShowFilterItem }}
                                 InputsController={InputsController}
                             />
                             <FilterItem
-                                type={"selectaddiction"}
-                                label="Authors"
+                                type={"selecttext"}
+                                label="Author"
                                 state={{ showFilterItem, setShowFilterItem }}
                                 InputsController={InputsController}
                             />
                             <FilterItem
-                                type={"selectaddiction"}
-                                label="Artists"
+                                type={"selecttext"}
+                                label="Artist"
                                 state={{ showFilterItem, setShowFilterItem }}
                                 InputsController={InputsController}
                             />
diff --git a/front-end/src/Shared/api/FetchTitle.ts b/front-end/src/Shared/api/FetchTitle.ts
index 5866cd4..cd7820a 100644
--- a/front-end/src/Shared/api/FetchTitle.ts
+++ b/front-end/src/Shared/api/FetchTitle.ts
@@ -13,14 +13,14 @@ export async function GetTitleById(id: number) {
 
 export async function GetTitlesByFilters(data: {
     name?: string,
-    authors?: string[],
-    artists?: string[],
+    author?: string,
+    artist?: string,
     genresIds?: number[] | string[],
     themesIds?: number[] | string[],
+    statusIds?: number[] | string[],
+    demographicIds?: number[] | string[],
+    contentRatingIds?: number[] | string[],
     publicationYear?: number | string,
-    statusId?: number | string,
-    demographicId?: number | string,
-    contentRatingId?: number | string,
 }) {
 
     const params = new URLSearchParams()
@@ -28,12 +28,13 @@ export async function GetTitlesByFilters(data: {
         if (value === undefined || value === null || value === '') return
 
         if (Array.isArray(value)) {
+            console.log(key)
+            console.log(value)
             value.forEach(v => params.append(key, String(v)))
         } else {
             params.append(key, String(value))
         }
     })
     const query = params.toString()
-
     return await PerformFetch<title[]>({ url: `/title/search?${query}` })
 }
diff --git a/front-end/src/Shared/form/FormController.ts b/front-end/src/Shared/form/FormController.ts
index ece1a06..d335e2d 100644
--- a/front-end/src/Shared/form/FormController.ts
+++ b/front-end/src/Shared/form/FormController.ts
@@ -30,6 +30,7 @@ export function useFormController(config: config) {
         changeValue: changeValue,
         data: data,
     }
+    
     function onChange(event: React.ChangeEvent<HTMLInputElement>) {
         const { name, value, type } = event.target
 
diff --git a/front-end/src/Shared/utils/isPlainObject.ts b/front-end/src/Shared/utils/isPlainObject.ts
new file mode 100644
index 0000000..d90faaa
--- /dev/null
+++ b/front-end/src/Shared/utils/isPlainObject.ts
@@ -0,0 +1,8 @@
+export default function isPlainObject(value: unknown): value is Record<string, unknown> {
+    return (
+        typeof value === "object" &&
+        value !== null &&
+        !Array.isArray(value) &&
+        Object.getPrototypeOf(value) === Object.prototype
+    );
+}
\ No newline at end of file
diff --git a/front-end/src/Shared/utils/staticMapper.ts b/front-end/src/Shared/utils/staticHandler.ts
similarity index 55%
rename from front-end/src/Shared/utils/staticMapper.ts
rename to front-end/src/Shared/utils/staticHandler.ts
index 0323425..7ec951e 100644
--- a/front-end/src/Shared/utils/staticMapper.ts
+++ b/front-end/src/Shared/utils/staticHandler.ts
@@ -7,10 +7,17 @@ const staticData = await GetAllStatic()
 /**
  * **Converts a static-data ID (e.g., genre, theme, etc.) into a human-readable name.**
  */
-export default function staticMapper(category: keyof typeof staticData, id: number) {
-    if(!staticData[category]) {
+export function staticMapper(category: keyof typeof staticData, id: number) {
+    if (!staticData[category]) {
         console.warn('Warn: staticMapper, category could not be found, value:', category)
         return
     }
     return staticData[category].find(item => item.id === id)?.name;
-}
\ No newline at end of file
+}
+
+/**
+ * **Returns all keys from the object returned by `GetAllStatic()`**
+ */
+export function getAllStaticKeys(): (keyof typeof staticData)[] {
+    return Object.keys(staticData) as (keyof typeof staticData)[]
+}
